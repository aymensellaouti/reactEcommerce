import React from 'react';
import {withRouter} from 'react-router-dom';
const Item = (props) => {
    return (
        <div>
           <h1>Item</h1>
        </div>
    );
};

export default withRouter(Item);
